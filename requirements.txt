# Core dependencies
reportlab>=4.0.0
PySimpleGUI>=4.60.0

# Accounting / Database
# sqlite3 is built into Python stdlib

# PDF rendering (optional alternatives)
# WeasyPrint>=50.0  # Use instead of reportlab if you prefer HTML-to-PDF

# Build tools
pyinstaller>=6.0.0